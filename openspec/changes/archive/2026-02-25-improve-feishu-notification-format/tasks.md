## 1. Body 解析功能

- [x] 1.1 在 common.sh 中添加 `parse_body_fields()` 函数，解析 `Key: Value` 格式
- [x] 1.2 添加 `get_field_icon()` 函数，实现字段图标映射

## 2. 卡片构建功能

- [x] 3.1 重构 `send_feishu_notification()` 函数，使用解析后的字段构建结构化卡片
- [x] 3.2 添加分隔线元素增强视觉层次
- [x] 3.3 实现解析失败降级逻辑，保证纯文本回退

## 3. 测试与部署

- [x] 4.1 使用 curl 测试新的卡片格式
- [x] 4.2 执行备份重装，验证全局安装
- [x] 4.3 触发各类通知，验证实际效果
