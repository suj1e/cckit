# No Spec Changes

This change is a purely organizational restructuring that:
- Moves OpenSpec files from `hooks/barnhk/openspec/` to `cckit/openspec/`
- Does not introduce new capabilities
- Does not modify existing capability requirements

No spec files are needed for this change.
